version: '3.5'

services:
  bull_mon:
    build: .
    env_file:
      - ../.env
    ports:
      - '80:4567'
    volumes:
      - ./:/app
      - /app/node_modules
    
networks:
  default:
    external:
      name: cw